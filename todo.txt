Next:
Step 1 would be Create browser window, with tab logic

then step 2 would be a home page within in the browser that would show 
(like in opera GX a Essential List / Pin Website List on the front page of the browser showing the sites the user can visit
(Github, LinkedIn, Fiverr))

then step 3 would be to build those sites

then step 4 would be to open those sites that i built inside the browser 
(easy just display the widget inside the browser tree / widget)

notes window
able to close system status

=========
Features:
=========

!! Windows
hover over close ('x') button turns red

!! Terminal
When pressing ARROW_UP in terminal, it copies the last command that was typed
tab for completing string in terminal window
Command in terminal gives quick description
Command --all gives all the data possible
bar cursor in terminal

============
Later Fixes:
============

!! Windows
Drag icon going past max size (maybe delta var issue)
There shouldn't be a max size for windows